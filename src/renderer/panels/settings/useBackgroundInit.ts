/**
 * Re-export from features/sessions/hooks where this hook now lives.
 * useBackgroundInit handles session initialization, not settings configuration.
 */
export { useBackgroundInit } from '../../features/sessions/hooks/useBackgroundInit'
